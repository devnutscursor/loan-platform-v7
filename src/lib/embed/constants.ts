/** Added to rate/APR in third-party embed widget (not stored in DB). */
export const EMBED_RATE_MARKUP = Number(process.env.EMBED_RATE_MARKUP ?? '0.125');

/** Default avatar ring / accent on dark embed card. */
export const DEFAULT_EMBED_ACCENT_COLOR = '#c9a227';
