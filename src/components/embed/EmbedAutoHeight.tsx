'use client';

/** Shared message type for iframe auto-resize (parent listens for this). */
export const EMBED_RESIZE_MESSAGE_TYPE = 'ratecaddy-embed-resize';

/** @deprecated Use EmbedDocumentSync — kept as re-export for imports. */
export default function EmbedAutoHeight() {
  return null;
}
